{
  "name": "elliot",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "example"
    ]
  },
  "scripts": {
    "dev": "yarn preconstruct dev && yarn workspace example dev",
    "build": "yarn preconstruct build && yarn workspace example build",
    "start": "yarn workspace example start"
  },
  "devDependencies": {
    "@preconstruct/cli": "^2.0.0",
    "@svgr/cli": "^5.5.0",
    "@types/node": "^14.14.10",
    "@types/react": "^17.0.0",
    "@types/react-beautiful-dnd": "^13.0.0",
    "@types/react-dom": "^17.0.0",
    "@types/uuid": "^8.3.0",
    "typescript": "^4.1.2"
  },
  "preconstruct": {
    "packages": [
      "packages/*"
    ]
  }
}
